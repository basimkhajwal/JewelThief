var Game = Game || {};
var Engine = Engine || {};

Game.States = Game.States || {};

Game.States.Menu = (function () {
    "use strict";

    var state = Engine.GameState.create();

    return state;

}());
