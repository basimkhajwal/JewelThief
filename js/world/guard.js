var Engine = Engine || {};
var Game = Game || {};

Game.World = Game.World || {};

Game.World.Guard = function (x, y) {
    "use strict";

    return {

        update: function (delta) {

        },

        render: function (canvas) {

        },

        getX: function () {
            return x;
        },

        getY: function () {
            return y;
        },

        getWidth: function () {
            return 40;
        },

        getHeight: function () {
            return 40;
        }
    };

};
